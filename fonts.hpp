#pragma once

#include "assets.hpp"

inline const blit::Font tall_font(asset_tall_font);

inline const blit::Font menu_font(asset_menu_font);

// only numbers
inline const blit::Font big_number_font(asset_big_number_font);